## Packages
framer-motion | Smooth animations for page transitions and product cards
lucide-react | Beautiful icons for UI elements
wouter | Lightweight routing
clsx | Conditional class merging
tailwind-merge | Intelligent tailwind class merging
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
zod | Schema validation

## Notes
- Using Google Fonts: 'Outfit' for headings and 'Inter' for body.
- Images will be placeholder via placehold.co or Unsplash for now.
- Auth is handled via generic Replit Auth (already set up in backend).
- Cart state is managed via server-state (TanStack Query) for persistence across devices.
